import React from 'react';

export function BottomConsole({ open, onOpenChange, logs, qiskit }: {
  open: boolean;
  onOpenChange: (o: boolean) => void;
  logs: string[];
  qiskit: string;
}) {
  return (
    <div>
      {/* BottomConsole implementation */}
    </div>
  );
}
